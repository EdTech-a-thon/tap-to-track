// Public, like the homepage: a teacher deciding whether to trust the tool should be
// able to read this without JavaScript. See +layout.ts, which switches SSR off for
// the app behind /chart.
export const ssr = true;
