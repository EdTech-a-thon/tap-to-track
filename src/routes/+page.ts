// The one page a search engine and a curious teacher should be able to read without
// running any JavaScript, so it opts back in to server rendering. See +layout.ts, which
// switches it off for the app behind /chart.
export const ssr = true;
